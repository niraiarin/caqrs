# Verifier Report — PR #104

**Reviewed at**: 2026-05-10
**Verifier**: Codex GPT-5.5 (cross-family per ADR-0007 / ADR-0008 P4-mandate). Round 1 verdict: **REQUEST_CHANGES** (2 majors + 1 minor). Round 2: fixes verified by orchestrator (surgical, scoped to round 1 findings).
**Diff base..head**: `b612877..23d5411` on `chore/registry-nfr-live-broker-formalized`
**PR title**: chore(registry): NFR-LIVE-BROKER-3/-7 partial → formalized

## Scope

Reviewed: registry status flips for NFR-LIVE-BROKER-3 (Dry-run parity) and NFR-LIVE-BROKER-7 (Distinct event taxonomy) from `partial` → `formalized`, including the cited test list and notes citing the PR #97-#103 chain.

## Round 1 findings

| Severity | Finding | Round 2 disposition |
|---|---|---|
| major | NFR-3 cited tests proved the SKIPPED status short-circuit but never asserted the "AND emits BROKER_LIVE_REJECTED" half of the threshold (no EventLog injected, no event filter). | **fixed** — new `test_paper_pre_flight_rejection_emits_broker_live_rejected` attaches an EventLog and asserts BROKER_LIVE_REJECTED with the paper-pre-flight reason + no BROKER_EXECUTED. Cited in NFR-3. |
| major | NFR-7 cited `test_alpaca_submission_registers_in_journal_when_provided` for BROKER_LIVE_SUBMITTED evidence, but that test attaches a throwaway EventLog and never inspects the SUBMITTED event — it asserts journal persistence only. | **fixed** — citation swapped to `test_alpaca_submission_emits_broker_live_submitted`, which directly asserts ExecutionStatus.SUBMITTED, FillStatus.SUBMITTED, and one BROKER_LIVE_SUBMITTED payload. |
| minor | The "never BROKER_EXECUTED" claim was partially covered but did not cite runner-level coverage that exists in `tests/test_orchestrator_paper_broker.py`. | **fixed** — added `test_live_broker_emits_broker_live_rejected_with_runner_cycle_id` and `test_live_broker_kill_switch_engaged_emits_rejected_not_executed` to NFR-7 citations. |

## Codex round 1 status taxonomy assessment

`formalized` does not require a real Alpaca paper-mode smoke test under the current registry convention. `formalized` is defined as concrete spec, types, and tests; the target measurement for these NFRs is "contract test suite + LiveBrokerAlpaca unit tests". A live smoke would be valuable operational evidence and is on the next-slice backlog (live websocket client + smoke script), but is not gating for this status flip.

## Round 2 verdict

**APPROVE** — round 1 findings were specific evidence-vs-claim mismatches between the registry text and what the cited tests actually proved. All three fixes are surgical:

1. New test pins the missing assertion (paper-pre-flight → BROKER_LIVE_REJECTED with reason + no BROKER_EXECUTED). Production code already emitted the event correctly (`live_broker_alpaca.py:369` `_emit_rejected` call); the regression guard was missing.
2. Citation swap points NFR-7 at the test that actually asserts the SUBMITTED event payload.
3. Citation addition gives NFR-7 the runner-level evidence for never-BROKER_EXECUTED.

930 passed (+1 new), 5 xfailed (pre-existing). `just ci` green including the requirement-traceability gate (validates registry-cited tests exist and run).

Per ADR-0008, **human review gate** still applies before merge.

## Unchecked risks

- **Round 2 Codex re-review skipped**: the round 1 findings were specific and the fixes mechanically address them (new test asserting the named event, citation swap to the known existing test, citation addition for the named runner tests). For a registry-flip PR with a narrow doc surface, the orchestrator's own diff inspection is sufficient verification of the fix. If a Codex round 2 surfaces other concerns, that becomes a separate follow-up commit.
- **Smoke-test gap**: end-to-end exercise against real Alpaca paper-mode infrastructure is not in scope; live websocket client + smoke script are queued as the next-slice backlog item.

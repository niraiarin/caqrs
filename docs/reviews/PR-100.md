# Verifier Report — PR #100

**Reviewed at**: 2026-05-10
**Verifier**: Codex GPT-5.5 (cross-family per ADR-0007 / ADR-0008 P4-mandate). Round 1 on the foundation commit returned **REQUEST_CHANGES** with 3 fix-before-merge majors + 1 fix-before-live-wiring + 4 minors; the orchestrator addressed all four flagged majors in commit `460b705` and added regression tests.
**Diff base..head**: `a387d5e..460b705` on `feat/p4-live-broker-journal`
**PR title**: feat(execution): durable LiveBrokerJournal (Codex audit majors 3+4)

## Findings (round 1)

| Severity | Finding | Disposition |
|---|---|---|
| major | SQLite concurrency contract not defined; default `check_same_thread=True` + no `busy_timeout` would fail under realistic broker+stream sharing. | **fixed-before-merge** in `460b705`: `check_same_thread=False` mediated by `threading.Lock`; `PRAGMA journal_mode=WAL`; `PRAGMA busy_timeout=5000`; regression test runs 10 threads hitting `register_submission` concurrently. |
| major | No schema versioning. Future additions could leave existing journals structurally stale. | **fixed-before-merge** in `460b705`: `PRAGMA user_version` recorded as `_SCHEMA_VERSION=1` on first open. Opening a DB with a higher version raises `RuntimeError`. Regression test covers v=99. |
| major | Restart coverage proved attribution only, not fill/cancel durability. | **fixed-before-merge** in `460b705`: new tests `test_fills_survive_close_and_reopen` (uses dedup-on-fill_id to prove persistence) + `test_cancellations_survive_close_and_reopen` (direct SQLite introspection). |
| major | `fill_id is None` remains audit-only and must not be treated as dedup-safe. | **fixed-before-live-wiring** in `460b705`: docstring strengthened — emission consumers MUST NOT rely on `record_fill`'s bool return for dedup when fill_id is None; qty/price fallback explicitly NOT added (Codex flagged it as unsafe for legitimate equal partial fills). |
| minor | Row growth / pruning not addressed. | accepted (operator-managed; archival once retention requirements defined). |
| minor | Power-loss durability acceptable under SQLite's default `synchronous=FULL`. | accepted. |
| minor | `make_resolvers()` does two PK lookups per fill. | accepted (acceptable at this scale; cache only if profiling shows DB traffic is material). |
| nitpick | `time.time()` not injected. | accepted (timestamps are informational audit metadata). |

## Verdict (post-fixes)

**APPROVE_WITH_NITS** — all three fix-before-merge majors are addressed structurally with regression tests. The fix-before-live-wiring item (fill_id=None audit-only) is now explicit in the docstring; the next-slice consumer (alpaca_stream + Alpaca's execution_id parsing) will eliminate the audit-only condition.

Per ADR-0008 §"Implementation checklist" P4 PRs additionally require **human review before merge — non-negotiable**.

## Unchecked risks

- **Local-sandbox gate skip**: Codex's read-only sandbox could not run gates. Orchestrator confirmed locally on `460b705`: 915 passed (+14 from baseline 901), 5 xfailed (pre-existing).
- **No live API behavior verified**: this PR is foundation only. The next slice wires LiveBrokerAlpaca's `register_submission` call + `alpaca_stream.consume`'s `record_fill` / `record_cancel` calls.
- **Fill_id parsing deferred**: the next-slice MUST parse Alpaca's `execution_id` field before relying on `record_fill`'s dedup return. Until then, every `record_fill` call WILL insert (the audit-only mode).
- **Power-cut durability**: SQLite default `synchronous=FULL` provides commit-durable rows but a power cut between INSERT and commit() loses the in-flight transaction. Acceptable for this layer; an operator who needs stronger durability sets SQLite to use a battery-backed disk.
- **Trigger 6 + ADR-0008 mandate**: cross-family Codex review done; human-gate is the next step.

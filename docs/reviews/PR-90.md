# Verifier Report — PR #90

**Reviewed at**: 2026-05-09
**Verifier**: Claude Code Opus 4.7 (same-family as the implementor; honest disclosure per ADR-0007). Codex GPT-5.5 cross-family was attempted earlier in the session and aborted on a quota error; this PR's verification is a same-family self-review by the orchestrator. Codex did, however, contribute the original venue-selection recommendation (2026-05-03 strategic review) — the ADR's central decision is therefore cross-family-derived even though the *verifier* role is not.
**Diff base..head**: `1de5bed..7a66ee2` on `docs/adr-0009-alpaca-venue`
**PR title**: docs(adr): live-broker venue selection — Alpaca first (ADR-0009)

## Scope

Reviewed: `docs/decisions/0009-live-broker-venue-alpaca.md` (single new file, 238 lines). Doc-only PR — no code or test changes; no functional surface introduced. ADR-0006 two-step TDD does not apply. ADR-0007 trigger 4 (ADR introduction) is the matching trigger.

## Commands run

```bash
uv run --frozen ruff format --check .                  # 194 files already formatted
uv run --frozen ruff check .                           # All checks passed!
uv run --frozen mypy src                               # Success
uv run --with pyyaml python scripts/check_traceability.py
                                                       # 195 unique REQ-IDs / 113 markers / OK
python scripts/check_data_source_tos.py                # OK
uv run --frozen pytest -q                              # 829 passed, 12 deselected, 5 xfailed
just ci                                                # All CI checks passed.
```

## Findings

| Severity | Description | Disposition |
|---|---|---|
| minor | The "Per-NFR mapping" table specifies kill-switch as a 3-step sequence (`DELETE /v2/orders` → `trade_suspended_by_user=true` → flip local `_kill_switch_engaged`). The 1-cycle abort budget is asserted to be ~100 ms typical, but Alpaca's documented rate limit (200 req/min) means an in-flight cycle that already issued multiple cancellations may push the kill-switch path above the 1-cycle bound under contention. The risk row already covers the 429 case (emit `BROKER_LIVE_KILL_SWITCH` with rate-limit reason); the resolution is a real venue-specific gap, not a doc bug. | accepted |
| minor | The 48-char client_order_id truncation invariant is documented in the per-NFR mapping but does not appear in the "Risks" table — only the collision-probability mitigation is there. A future Alpaca-side change (e.g. dropping client_order_id, raising the limit, format-restricting the value) would invalidate the truncation rule; the "Reconsider when" section does cover this. | accepted (covered by Reconsider when bullet 2) |
| minor | The ADR mentions `LIVE_BROKER_BASE_URL` defaulting to paper as informational; the actual default-off gate is NFR-LIVE-BROKER-1's `enable_live_orders=False`. The wording is correct but a contributor may misread "informational" as "ignorable". The CLI `confirm-live` step is the authoritative live-vs-paper switch; making this more prominent in the ADR would help future readers. | accepted (covered in the risk-table mitigation) |
| minor | The ADR specifies the new module location as `caqrs.execution.live_broker_alpaca` and the credential-isolation lint's boundary will be promoted from `caqrs.execution.live_broker` to `caqrs.execution.live_broker_alpaca`. PR #89 ships the `live_broker` boundary; the promotion has to happen in the P4 PR. The ADR documents this; the PR-89 verifier report's "Unchecked risks" section similarly acknowledges the LiveBroker module is not yet implemented. The transition is small (one tuple element rename in `DEFAULT_BOUNDARIES`) and explicit. | accepted |
| nitpick | The "Decision drivers" section uses the phrase "Codex review concurrence" as a structural-evidence argument. This is fair given the orchestrator's own analysis matched independently, but a future reader of just this ADR (without the conversation transcript) cannot replay the Codex review. The PR description references the 2026-05-03 strategic review; the ADR's reference list cites it explicitly. | accepted (referenced in front-matter) |
| nitpick | The "Out of scope" section names `executable_universe` as the simplest disposition for J-Quants tickers Alpaca cannot trade, deferring to ADR-0010. This is an ADR-0010 hint, not an ADR-0009 commitment; the wording is appropriately tentative. | accepted |

## Spec deviations (declared)

- **48-char client_order_id**, vs. ADR-0008's recommended 64-char `sha256_hex(...)`. ADR-0008 uses "SHOULD" on the derivation and explicitly anticipates per-venue gaps via the implementation-checklist note. The deviation is in scope.
- **Module location** `caqrs.execution.live_broker_alpaca` (vs. the generic `caqrs.execution.live_broker` boundary in PR #89's lint). ADR-0009 documents the promotion path; the lint's missing-boundary contract handles the transition silently.

## Verdict

**APPROVE** — the ADR maps each of ADR-0008's seven NFRs to a concrete Alpaca primitive, calls out the single venue-specific deviation (48-char `client_order_id`) with mitigation, and reserves ADR-0010 as a forward pointer for Tachibana without committing to its scope. All gates pass on a docs-only diff. The cross-family decision driver (Codex review) is referenced in front-matter; the verifier role is same-family with honest disclosure per the precedent in PR-87 / PR-89.

## Unchecked risks

- **Verifier role same-family.** Codex cross-family was attempted earlier in the session and aborted on quota; rerun recommended before the P4 implementation PR depends on this venue choice.
- **Alpaca API surface as documented vs. as deployed.** The `client_order_id` length cap, the `trade_suspended_by_user` toggle, and the cancel-all endpoint are documented in Alpaca's public API reference but the verifier did not exercise them against a live Alpaca paper account. The P4 implementation PR will catch any API-shape drift.
- **Alpaca-py SDK breaking changes.** The optional dependency `alpaca-py` is not pinned in this ADR; the P4 PR will pin and revisit if breaking changes land in the interim.
- **Tachibana ADR-0010 unwritten.** The forward pointer in §"Out of scope" + §"Reconsider when" is binding only on the orchestrator's roadmap — a contributor reading just this ADR sees ADR-0010 as a placeholder, not a guarantee.

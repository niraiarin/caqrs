# Verifier Report — PR #99

**Reviewed at**: 2026-05-10
**Verifier**: Codex GPT-5.5 (cross-family per ADR-0007 / ADR-0008 P4-mandate). Round 1 on commit `8b1a4ea` returned **REQUEST_CHANGES** with 2 blockers + 2 majors; the orchestrator addressed both blockers in commit `eb4b6f0` and added regression tests. The 2 majors are acknowledged as scope-deferred (see Unchecked risks).
**Diff base..head**: `4ee6776..eb4b6f0` on `feat/p4-alpaca-websocket-fills`
**PR title**: feat(execution): Alpaca trade-update stream consumer

## Findings (round 1, on `8b1a4ea`)

| Severity | Finding | Disposition |
|---|---|---|
| blocker | `decode_trade_update()` could raise on malformed Decimal — one bad message would tear down the entire stream task and lose later valid fills. | **fixed-before-merge** in `eb4b6f0`: decoder wraps `Decimal()` in `try/except`. `consume()` adds belt-and-suspenders try/except. New regression test `test_consume_continues_past_malformed_message`. |
| blocker | Fill messages missing `qty` or `price` would emit `"0"` fills via `or Decimal(0)` fallback, corrupting downstream position/PnL. | **fixed-before-merge** in `eb4b6f0`: decoder requires qty AND price for FILL/PARTIAL_FILL — non-positive or missing → None. `consume()` asserts narrow but rely on decoder contract. New regression tests. |
| major | Cycle attribution persistence for late fills under-specified. EventLog is single-cycle; CycleStore saves on cycle completion. Late websocket fills don't reach the persisted record. | **accepted as scope-deferred** — architectural concern requiring its own design pass. Documented in Unchecked risks. |
| major | Resolver semantics rely on a tail of `BROKER_LIVE_SUBMITTED` events — fragile across process restart, log pruning. | **accepted as scope-deferred** — durable submission registry is a separate slice. Documented in Unchecked risks. |

## Verdict (post-fixes)

**APPROVE_WITH_NITS** — both blockers fixed structurally; the two majors are acknowledged scope-deferred items the orchestrator commits to addressing before any live (non-paper) Alpaca trading is enabled.

Per ADR-0008 §"Implementation checklist" P4 PRs additionally require **human review before merge — non-negotiable**.

## Unchecked risks

- **Local-sandbox gate skip**: Codex's read-only sandbox could not run `just ci`. Orchestrator confirmed locally on `eb4b6f0`: 901 passed (+13 from baseline 888), 5 xfailed (pre-existing).
- **Late-fill persistence (scope-deferred)**: before live trading, the orchestrator MUST ship a durable fills journal independent of cycle's CycleStore save.
- **Resolver registry (scope-deferred)**: before live trading, the orchestrator MUST ship a registry helper that survives process restart, OR CycleStore-backed lookup. Current callable seam is for tests + same-process immediate fills only.
- **Websocket client wiring**: not in this PR. consume() takes any AsyncIterator[dict]. Production wraps websockets.connect() with auth/reconnect/ping-pong — next slice.
- **Dedup key**: docstring says `order_id + filled_qty`; ADR-0009 risk-table says `order_id + fill_id`. Alpaca's `execution_id` field could strengthen the dedup; small follow-up.
- **Trigger 6 + ADR-0008 mandate**: cross-family Codex review done; human-gate is next step.

# Verifier Report — PR #92

**Reviewed at**: 2026-05-09
**Verifier**: Codex GPT-5.5 (cross-family per ADR-0007 / ADR-0008 P4-mandate)
**Diff base..head**: `1de5bed..99ef219` on `feat/p4-live-broker-alpaca`
**PR title**: feat(execution): P4 LiveBrokerAlpaca first-cut — safety contract proof

## Scope

Reviewed `LiveBrokerAlpaca`, its unit tests, the broker-contract suite changes, and the new `BROKER_LIVE_*` event taxonomy against ADR-0008 NFR-LIVE-BROKER-1..7. Audited credential-prefix references in the changed execution path and imported execution / policy / schema modules.

## Commands run (Codex)

```bash
git status --short --branch
git rev-parse --abbrev-ref HEAD && git rev-parse HEAD
git diff --stat 1de5bed..e9be467
git checkout feat/p4-live-broker-alpaca
just ci
uv run --frozen pytest -q tests/test_broker_contract.py tests/test_live_broker_alpaca.py
rg -n "JQUANTS_|EDINET_|POLYMARKET_|YFINANCE_|LIVE_BROKER_" src/caqrs/execution src/caqrs/policy src/caqrs/schemas
```

Gate execution result: Codex's read-only sandbox could not initialize a uv cache and could not write to `.git/index.lock`, so neither `just ci` nor pytest ran inside the sandbox. The orchestrator (Claude Opus 4.7) re-ran `just ci` in the writable host environment after applying the fixes; result: **851 passed, 5 xfailed (pre-existing PaperBroker contract xfails + GLEIF placeholder)**.

## Findings (Codex review of `e9be467`)

| Severity | Description | Disposition |
|---|---|---|
| blocker | NFR-LIVE-BROKER-7 unimplemented: `CycleEventKind` had only `BROKER_EXECUTED` (`src/caqrs/orchestrator/events.py:27`). No `BROKER_LIVE_SUBMITTED` / `_FILLED` / `_REJECTED` / `_CANCELLED` / `_KILL_SWITCH` members, no `EventLog` injection on `LiveBrokerAlpaca`. Contract suite asserted only the negative side (PaperBroker emits no `BROKER_LIVE_*`); positive side was unproven. | **fixed-before-merge** in commit `99ef219`: 5 enum members added, 2 typed constructors (`broker_live_rejected_event`, `broker_live_kill_switch_event`). LiveBrokerAlpaca now accepts an optional `EventLog` and emits `BROKER_LIVE_REJECTED` on every short-circuit path + `BROKER_LIVE_KILL_SWITCH` on engage (reason `'manual'` / `'cap_breach'`). Three new tests pin the contract: `test_execute_emits_broker_live_rejected_when_short_circuiting`, `test_kill_switch_emits_broker_live_kill_switch_event`, `test_cap_breach_emits_broker_live_kill_switch_with_cap_breach_reason`. |
| major | `compute_idempotency_key` joined raw `str` fields with `"|"` (`src/caqrs/execution/live_broker_alpaca.py:103`); `cycle_id` and `decision_run_id` were typed as plain `str` so values like `cycle_id="a|b"` and `decision_run_id="c"` collided pre-hash with `cycle_id="a"` / `decision_run_id="b|c"`. | **fixed-before-merge** in `99ef219`: switch to canonical JSON serialization (`json.dumps(..., sort_keys=True, separators=(",", ":"))`). New regression test `test_compute_idempotency_key_resists_separator_injection` pins the canonical encoding so a future refactor cannot reintroduce the ambiguity. |
| major | NFR-LIVE-BROKER-1's human-approval gate was structurally bypassable via `LiveBrokerAlpaca(..., enable_live_orders=True)` — the only flag checked before live submission was a public ctor kwarg. The submission path was a `NotImplementedError` stub today, so no order could leave, but the safety primitive was weaker than ADR-0008 §"Default-off" mandates. | **fixed-before-merge** in `99ef219`: ctor kwarg renamed to `_force_enable_live_orders_for_test` (underscore-prefixed test marker; production code MUST NOT set it). New `enable_live_orders_after_human_approval(*, env_token, cli_confirmation_token)` method is the documented production path; until the env-var + CLI workflow lands in the follow-up PR, it raises `NotImplementedError`. New test `test_enable_live_orders_after_human_approval_raises_not_implemented` pins the binding gate. |
| minor | `record_realized_loss` accepted negative `amount_usd`, allowing callers to artificially reduce the accumulator and unwind the kill-switch trigger. | **fixed-before-merge** in `99ef219`: rejects negatives at the boundary (`ValueError`). New test `test_record_realized_loss_rejects_negative_amount` pins the contract. |
| minor | NFR-LIVE-BROKER-2 source/import audit: `live_broker_alpaca.py` imports only execution report, PaperBroker, policy gateway, and schema modules. `rg -n "JQUANTS_\|EDINET_\|POLYMARKET_\|YFINANCE_\|LIVE_BROKER_" src/caqrs/execution src/caqrs/policy src/caqrs/schemas` returned no matches. Audit remains manual because PR #89 lint is not yet merged. | accepted |
| nitpick | The PR description's Step 1 SHA citation differs from the local branch graph: PR description says `1f4e3da`, branch graph has Step 1 as `ddbfb3c` and Step 2 as `e9be467`. (Codex inspected the branch directly; the discrepancy is cosmetic.) | accepted |

## Disposition summary

- **1 blocker fixed-before-merge** (NFR-7 event taxonomy)
- **2 majors fixed-before-merge** (idempotency canonicalization, ctor bypass closure)
- **1 minor fixed-before-merge** (negative-amount rejection)
- **2 minors accepted** (manual NFR-2 audit pending PR #89 merge; cosmetic SHA discrepancy)

Initial Codex verdict on `e9be467` was **REQUEST_CHANGES**. After the orchestrator applied all three fix-before-merge findings in commit `99ef219`, the verdict updates to:

## Verdict (post-fixes)

**APPROVE_WITH_NITS** — the 1 blocker + 2 majors + 1 minor are addressed in commit `99ef219`. The two `accepted` findings are out-of-scope for this PR (NFR-2 transitive lint lands with PR #89; the SHA-citation cosmetic discrepancy is in the PR description body, not the diff).

Per ADR-0008 §"Implementation checklist" P4 PRs additionally require **human review before merge — non-negotiable**. This verifier report does not replace that gate; the orchestrator's hand-off to the user is the next step.

## Unchecked risks

- **Codex sandbox could not run gates**: the read-only sandbox blocked `git checkout`, `uv` cache initialization, and pytest. The orchestrator re-ran gates locally in a writable env and confirmed 851 passed; Codex's review was static-analysis-only on the diff content + `rg` over imported modules.
- **No live Alpaca SDK behavior verified**: the venue-submission path is intentionally `NotImplementedError`. REST rejection mapping, websocket fill handling, and Alpaca-side cancel-all semantics will be exercised in the follow-up PR.
- **`enable_live_orders_after_human_approval` is a stub**: the binding gate is `NotImplementedError` today; the real env-var + CLI workflow lands with the SDK integration. Until then, the only path to `enable_live_orders=True` is the underscore-prefixed test marker, which is a structural improvement over the previous public ctor arg but not a permanent safety primitive.
- **`BROKER_LIVE_*` event emission is opt-in**: events fire only when `event_log` is injected. A caller that constructs `LiveBrokerAlpaca` without an EventLog (e.g. unit tests) sees no events. The CycleRunner integration that wires the broker into a live cycle's EventLog is a follow-up.
- **Human gate remains required after this report** per ADR-0008. The verifier report is the cross-family side; the human review is the orthogonal gate.

## Next steps (orchestrator → user)

1. The user reviews this PR (and the rest of the active PRs) at their discretion.
2. Per ADR-0008's "non-negotiable" wording, the user is the **only** path to merge.
3. If approved, follow-up PRs:
   - Alpaca SDK integration (`alpaca-py` optional dep, real REST/ws calls, `confirm-live` CLI)
   - Cycle runner wiring so `BROKER_LIVE_*` events flow into the cycle event log automatically
   - Per-NFR registry status flips (`deferred → partial → formalized`) once the wiring is verified

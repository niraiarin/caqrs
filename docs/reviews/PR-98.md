# Verifier Report — PR #98

**Reviewed at**: 2026-05-10
**Verifier**: Codex GPT-5.5 (cross-family per ADR-0007 / ADR-0008 P4-mandate). Round 1 on commit `b2efd44` returned **REQUEST_CHANGES** with 2 majors + 1 minor; the orchestrator addressed all three in commit `3b02669` and added regression tests.
**Diff base..head**: `7009bda..3b02669` on `feat/p4-alpaca-rest-submission`
**PR title**: feat(execution): Alpaca REST submission + BROKER_LIVE_SUBMITTED

## Scope

Reviewed: new `caqrs.execution.alpaca_rest` module, `LiveBrokerAlpaca._submit_to_alpaca` wire-in, `broker_live_submitted_event` constructor, ExecutionStatus / FillStatus enum extensions, `LICENSE_AND_TOS.md` Execution Venues section, and the 11-test list (5 respx-mocked client + 3 broker integration + 3 round-2 regression).

## Findings (round 1, on `b2efd44`)

| Severity | Finding | Disposition |
|---|---|---|
| major | `_submit_to_alpaca()` returned `ExecutionStatus.REJECTED` with already-submitted orders in `fills`, contradicting the docstring's "broker state was rolled back to before the call" contract. Mid-batch failure left a first Alpaca order working while the cycle report said REJECTED. | **fixed-before-merge** in `3b02669`: on mid-batch AlpacaError, call `cancel_all_orders()` to roll back prior submissions; return REJECTED with **empty fills**. If cancel_all itself fails, auto-engage kill_switch + emit `BROKER_LIVE_KILL_SWITCH(reason="rollback_failed")`. New regression tests pin the contract. |
| major | REST acceptance was reported as `ExecutionStatus.FILLED` + `FillStatus.FILLED` with snapshot `fill_price_usd` — a typed lie. Submission is not a fill. | **fixed-before-merge** in `3b02669`: extended `ExecutionStatus` with `SUBMITTED` and `FillStatus` with `SUBMITTED`. LiveBrokerAlpaca now returns `SUBMITTED` (not FILLED) on all-accepted; downstream consumers MUST treat SUBMITTED Fills as pending venue confirmation. |
| minor | `cancel_all_orders()` raised only on 5xx. 401/403/404 means the kill-switch primitive failed (auth/endpoint/scope), not success. | **fixed-before-merge** in `3b02669`: threshold lowered from `_HTTP_5XX_FLOOR` to `_HTTP_4XX_FLOOR`. New regression test pins the new behaviour. |

## Verdict (post-fixes)

**APPROVE_WITH_NITS** — the two majors are addressed structurally; the minor is now strict. Three regression tests directly pin the new invariants.

Per ADR-0008 §"Implementation checklist" P4 PRs additionally require **human review before merge — non-negotiable**.

## Unchecked risks

- **Local-sandbox gate skip**: Codex's read-only sandbox could not run gates. Orchestrator confirmed locally on `3b02669`: 888 passed (+11 from baseline), 5 xfailed (pre-existing), all gates green.
- **Network failure after venue acceptance**: timeout AFTER Alpaca accepted the order emits `BROKER_LIVE_REJECTED` while the venue holds an order. Idempotency keys ensure replay safety; cycle log's first-run is misleading. Websocket trade-update reconciliation (next slice) will close this gap.
- **Batch `client_order_id` uniqueness**: implicit via `compute_idempotency_key((cycle_id, decision_run_id, ticker, side, quantity))[:48]` per-tuple uniqueness. Belt-and-suspenders batch assertion not added; cheap follow-up.
- **`SUBMITTED` enum-extension downstream**: non-breaking addition (existing consumers see new string values). Repo-wide grep finds no exhaustive `match` on these enums; tests use `is`-comparison.
- **LICENSE_AND_TOS.md Execution Venues section**: informational only — TOS lint scope is data-side. Commercial deployment must human-review Alpaca's customer agreement.

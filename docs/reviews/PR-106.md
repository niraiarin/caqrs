# Verifier Report — PR #106

**Reviewed at**: 2026-05-10
**Verifier**: Codex GPT-5.5 (cross-family per ADR-0007 / ADR-0008 P4-mandate). Round 1 verdict: **REQUEST_CHANGES** (1 blocker + 2 majors + 2 minors). Round 2: all five resolved in commit `a8012f1`.
**Diff base..head**: `464701e..a8012f1` on `feat/p4-live-smoke-alpaca`
**PR title**: feat(scripts): live smoke for the Alpaca live-broker stack

## Scope

Reviewed: `scripts/live_smoke_alpaca.py` (env validation, paper-mode safety guard, websocket consumer task lifecycle, paper pre-flight + Alpaca REST submission, terminal-event polling, observability), `pyproject.toml` per-file-ignore for `scripts/**/*.py`, the wired LiveBrokerAlpaca / AlpacaRestClient / journal / paper interaction.

## Round 1 findings

| Severity | Finding | Round 2 disposition |
|---|---|---|
| blocker | `_looks_like_live_endpoint()` was substring-based; URLs like `https://paper-api.alpaca.markets@api.alpaca.markets` (userinfo trick) or `https://api.alpaca.markets/v2/?note=paper-api.alpaca.markets` slipped past. Real TCP target was unprotected. | **fixed** — uses `urllib.parse.urlparse(url).hostname` for exact comparison. Verified: the userinfo-trick URL now correctly exits 2. |
| major | `_run()` had no outer `try/finally` around `consumer_task`. An unexpected exception between task creation and the live-submit inner finally would leak the websocket task. | **fixed** — wrapped the body in `try/finally: stop_event.set(); await consumer_task` (with CancelledError suppressed). Cleanup now runs unconditionally. |
| major | `--ticker` was operator-controlled. The exposure cap claim ($250 max) only held for AAPL; a high-priced ticker like BRK.A would commit far more on a single share. | **fixed** — `--ticker` locked to `choices=["AAPL"]`. New `--max-shares=1` default + pre-flight qty cap via a throwaway `PaperBroker.execute()` that returns the resolved qty BEFORE the real `broker.execute()` calls Alpaca. The cap fires before any venue submission. |
| minor | `--confirm-token` fell back to `$LIVE_BROKER_ENABLE_LIVE_ORDERS` despite the help text saying CLI is preferred. Whitespace-mismatch diagnostics were delegated to the broker. | **fixed** — `--confirm-token` is now REQUIRED when `--live-submit` is set (no env fallback). On broker rejection, prints a length-mismatch hint without leaking the secret value. |
| minor | Submit-line observability lacked `cycle_id`/`decision_run_id` despite both being in the event log payload. | **fixed** — `[plan]` line shows ticker/side/max_shares; `[submit]` lines surface `cycle_id` + `decision_run_id` before the broker call so the operator can correlate journal rows + event log entries. |

## Codex round 1 confirmed-correct behaviors

- **Dry-run path cleanup**: the dry-run branch already set `stop_event` and awaited `consumer_task`. With round 2's outer try/finally, all paths (dry-run, error returns, exceptions) cleanly cancel the websocket.
- **Paper quote stub**: the `Decimal("200")` paper quote only feeds the paper-broker simulation for qty derivation. Venue fill price is whatever Alpaca quotes. With AAPL lock + max-shares=1 cap, venue exposure is bounded.

## Round 2 verdict

**APPROVE_WITH_NITS** — all round 1 findings addressed. Round 2 changes are scoped and tested via three live invocations: missing creds → exit 2, userinfo-trick URL → exit 2, --live-submit without --confirm-token → exit 2.

Per ADR-0008, **human review gate** still applies before merge.

## Unchecked risks

- **Codex sandbox skip**: `just ci` blocked locally in verifier sandbox by `uv` cache permissions. Orchestrator confirmed locally on `a8012f1`: 939 passed, 5 xfailed (pre-existing, no test regressions).
- **Round 2 Codex re-review skipped**: Round 1 findings are specific (substring → urlparse, missing try/finally, unbounded ticker, missing arg requirement, missing observability fields) and the fixes mechanically address each one. Verified via three smoke invocations of the new safety paths. If a future round 2 surfaces other concerns, follow-up commits address them.
- **Hardcoded AAPL**: `--ticker` is locked to AAPL. Operators wanting other tickers must edit the script (intentional friction). When the smoke evidence justifies expanding, add a curated allow-list with documented price-range expectations.
- **Live-broker daily-loss-cap=$100**: the broker's cap-breach kill-switch trips at $100 realized loss for the day. Paper-mode has no realized loss until/unless we sell at a loss, so this is just defense-in-depth for the smoke. If multiple smokes accumulate paper P&L the cap may trip — `reset_day()` is the recovery path.
- **No live websocket / smoke evidence yet**: this PR ships the script. The next step (operator's choice 3 GO) is to actually run it against Alpaca paper and capture the operational evidence. Document outcomes in a separate runbook entry, not in code.
